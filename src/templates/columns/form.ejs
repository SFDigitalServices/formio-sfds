<div class="d-md-flex mr-md-n2">
  {% ctx.component.columns.forEach(function(column, index) { %}
    <div class="col-md-{{ column.width }} mr-md-2" ref="{{ ctx.columnKey }}">
      {{ ctx.columnComponents[index] }}
    </div>
  {% }) %}
</div>
