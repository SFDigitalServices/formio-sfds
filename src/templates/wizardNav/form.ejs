{% var button_classes = 'sfgov-button mt-0 btn-sm' %}
<div class="d-flex flex-justify-between" id="{{ ctx.wizardKey }}-nav">
  <div class="flex-auto">
    {% if (ctx.buttons.previous) { %}
      <button class="{{ button_classes }} sfgov-button-primary btn-prev mr-1" ref="{{ctx.wizardKey}}-previous">
        {{ ctx.t('Previous') }}
      </button>
    {% } %}
    {% if (ctx.buttons.next) { %}
      <button class="{{ button_classes }} sfgov-button-primary btn-next mr-1" ref="{{ctx.wizardKey}}-next">
        {{ ctx.t('Next') }}
      </button>
    {% } %}
    {% if (ctx.buttons.submit) { %}
      <button class="{{ button_classes }} sfgov-button-primary mr-1" ref="{{ctx.wizardKey}}-submit">
        {{ ctx.t('Submit') }}
      </button>
    {% } %}
  </div>

  {% if (ctx.buttons.cancel) { %}
    <div class="flex-self-end">
      <button class="{{ button_classes }} bg-none fg-bright-blue border-none u" ref="{{ctx.wizardKey}}-cancel">
        {{ ctx.t('Leave the form') }}
      </button>
    </div>
  {% } %}
</div>
