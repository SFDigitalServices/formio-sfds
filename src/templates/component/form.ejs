<div
  id="{{ ctx.id }}"
  class="{{ ctx.classes }}"
  {% if (ctx.styles) { %} style="{{ ctx.styles }}"{% } %}
  ref="component"
>
  {% if (ctx.visible) { %}
    {{ ctx.children }}
    <div ref="messageContainer"></div>
  {% } %}
</div>
