<div
  id="{{ ctx.id }}"
  class="{{ ctx.classes }}"
  {% if (ctx.styles) { %} style="{{ ctx.styles }}"{% } %}
  ref="component"
>
  {% if (ctx.visible) { %}
    {{ ctx.children }}
    <div class="messages" ref="messageContainer"></div>
  {% } %}
</div>
