{{ ctx.labelMarkup }}

{% const desc = ctx.t(ctx.component.description) %}
{% if (['datagrid'].indexOf(ctx.component.type) > -1) { %}
  {% if (desc) { %}
    <div class="fg-light-slate mt-1">
      {{ desc }}
    </div>
  {% } %}
{% } %}

{{ ctx.element }}

{% if (['address', 'checkbox', 'radio', 'review', 'selectboxes', 'well', 'datagrid'].indexOf(ctx.component.type) === -1) { %}
  {% if (desc) { %}
    <div class="fg-light-slate mt-1">
      {{ desc }}
    </div>
  {% } %}
{% } %}
