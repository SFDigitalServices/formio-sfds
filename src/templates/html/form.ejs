<{{ctx.tag}} class="{{ ctx.component.className }}" ref="html"
  {% ctx.attrs.forEach(function(attr) { %}
    {{attr.attr}}="{{attr.value}}"
  {% }) %}
>{{ ctx.t([`${ctx.component.key}_content`, `${ctx.component.key}.content`, ctx.content]) }}{% if (!ctx.singleTags || ctx.singleTags.indexOf(ctx.tag) === -1) { %}</{{ctx.tag}}>{% } %}
